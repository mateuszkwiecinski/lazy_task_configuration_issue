buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.3.31"
    }
}

apply plugin: 'kotlin'

tasks.register("dupa") {
     throw new GradleException("ERROR")
}
repositories {
    jcenter()
}

